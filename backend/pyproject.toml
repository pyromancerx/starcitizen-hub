[project]
name = "starcitizen-hub"
version = "0.1.0"
description = "Self-hosted social and logistics hub for Star Citizen communities"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
